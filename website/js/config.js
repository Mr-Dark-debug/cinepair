export const CONFIG = {
    github: {
        owner: 'facebook', // Default target: facebook/react as an example of a rich repo
        repo: 'react',
        // token: 'YOUR_GITHUB_TOKEN' // Add token here to avoid rate limits
    }
};